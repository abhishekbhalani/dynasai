{
  "name": "dynasai",
  "type": "module",
  "version": "0.1.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "preview:cf": "astro build && wrangler dev",
    "deploy": "astro build && wrangler deploy",
    "release": "node scripts/release.mjs",
    "release:dry": "node scripts/release.mjs --dry-run",
    "cicd:github": "node scripts/setup-github-cicd.mjs",
    "cf:check": "node scripts/check-cloudflare.mjs",
    "pages:create": "node scripts/create-pages.mjs"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.5",
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.2"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
