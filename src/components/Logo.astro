---
import { site } from '../config';

interface Props {
  variant?: 'header' | 'footer';
}

const { variant = 'header' } = Astro.props;
---

<a class={`brand brand--${variant}`} href="/" aria-label={`${site.name} home`}>
  <img
    class="brand-logo"
    src={site.logo}
    alt={site.name}
    width="181"
    height="50"
    decoding="async"
    fetchpriority={variant === 'header' ? 'high' : 'auto'}
  />
</a>
